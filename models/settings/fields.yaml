# ===================================
#  Form Field Definitions
# ===================================
fields:
    _hint:
        type: partial
        path: $/albrightlabs/twilio/models/settings/_hint.htm
tabs:
  fields:
      sid:
          label: SID
          tab: Gateway
      token:
          label: Token
          tab: Gateway
      number:
          label: Number
          comment: The from number used by your Twilio account. Numbers only, do not include +1.
          tab: Numbers
      forward:
          label: Foward
          comment: SMS messages will be fowarded here. Numbers only, do not include +1.
          tab: Numbers
