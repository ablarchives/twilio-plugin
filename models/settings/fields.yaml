# ===================================
#  Form Field Definitions
# ===================================
fields:
    _hint:
        type: partial
        path: $/albrightlabs/twilio/models/settings/_hint.htm
tabs:
  fields:
      sid:
          label: SID
          tab: Gateway
      token:
          label: Token
          tab: Gateway
      number:
          label: Number
          commentBelow: The from number used by your Twilio account. Numbers only, do not include +1.
          tab: Numbers
      forward:
          label: Foward
          commentBelow: SMS messages will be fowarded here. Numbers only, do not include +1.
          tab: Numbers
